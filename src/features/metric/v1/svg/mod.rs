pub mod coords;
pub mod db;
pub mod handler;
pub mod params;
pub mod text_generation;

// use axum::routing::get;
// use axum::Router;

// /// Создает маршрутизатор для SVG маршрутов.
// pub fn router() -> Router {
//     Router::new().route("/svg", get(handler::svg))
// }
